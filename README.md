### Generated by [PHP Docker IO](https://phpdocker.io/generator)

```
docker-compose up -d
```

Access the *-fpm container to setup the framworks.

The folders will be on the host as well.

The default port is 8081.
